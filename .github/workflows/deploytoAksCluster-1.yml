on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    
    - uses: actions/checkout@master
    
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Create ACR
      id: create-acr
      run: |
        az acr create -n frontendAngular -g frontendAngularb33c-rg --location "West Europe" --sku Standard --admin-enabled
        echo "::set-output name=acr_username::`az acr credential show -n frontendAngular --query username | xargs`"
        echo "::set-output name=acr_password::`az acr credential show -n frontendAngular --query passwords[0].value | xargs`"
        echo "::add-mask::`az acr credential show -n frontendAngular --query passwords[0].value | xargs`"

    - uses: azure/docker-login@v1
      with:
        login-server: frontendangular.azurecr.io
        username: ${{ steps.create-acr.outputs.acr_username }}
        password: ${{ steps.create-acr.outputs.acr_password }}

    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/frontend" -f  "frontend/Dockerfile" -t frontendangular.azurecr.io/onlineshop:${{ github.sha }}
        docker push frontendangular.azurecr.io/onlineshop:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      id: login
      with:
         kubeconfig: ${{ secrets.aks_onlineshop_kubeConfig }}
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep onlineshop9e3a | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace onlineshop9e3a`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: onlineshop9e3a
        container-registry-url: frontendangular.azurecr.io
        container-registry-username: ${{ steps.create-acr.outputs.acr_username }}
        container-registry-password: ${{ steps.create-acr.outputs.acr_password }}
        secret-name: onlineshopdockerauth
       
    - uses: azure/k8s-deploy@v1
      with:
        namespace: onlineshop9e3a
        manifests: |
          manifests/deployment-1.yml
          manifests/service-1.yml
        images: |
          frontendangular.azurecr.io/onlineshop:${{ github.sha }}
        imagepullsecrets: |
          onlineshopdockerauth