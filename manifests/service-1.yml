apiVersion: v1
kind: Service
metadata:
    name: "onlineshop"
    labels:
        app: "onlineshop"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "onlineshop"